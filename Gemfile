source 'http://rubygems.org'

# TODO: move this back to the gemspec when the new capybara is released as a gem
gem 'capybara', :git => 'git://github.com/jnicklas/capybara.git'

gemspec

group :development do
  gem 'rspec', '~> 2.0.0.beta.22'
  gem "sinatra"
  gem "mongrel", '~> 1.2.pre'
  gem "ruby-debug"
end
